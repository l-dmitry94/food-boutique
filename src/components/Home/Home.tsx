import Filters from 'components/Filters';
import Hero from 'components/Hero';

const Home = () => {
    return (
        <>
            <Hero />
            <Filters />
        </>
    );
};

export default Home;
